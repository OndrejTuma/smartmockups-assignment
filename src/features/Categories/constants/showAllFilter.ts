import type { FlattenFilterCategory } from '@features/Categories/components/Categories'

const showAllFilter: FlattenFilterCategory = {
  slug: '',
  title: 'Show all',
}

export default showAllFilter